name: badgo

on:
  push:
    paths:
    - 'badgo'
    - '.github/workflows/badgo.yml'

jobs:
  ubuntu:
    runs-on: ubuntu-18.04
    steps:
      - name: setup-go
        uses: actions/setup-go@matcher
        with:
          go-version: '^1.13.1'
      - run: echo $(pwd)
      - run: ls -la
      - run: cd badgo && ls -la
      - run: cd badgo && go build